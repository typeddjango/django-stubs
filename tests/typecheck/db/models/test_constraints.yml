- case: get_violation_error_message_str
  main: |
    from django.db.models.constraints import BaseConstraint

    class MyConstraint(BaseConstraint):

        def get_violation_error_message(self) -> str:
            return "Validation error message"

- case: get_violation_error_message_dict
  main: |
    from django.db.models.constraints import BaseConstraint

    class MyConstraint(BaseConstraint):

        def get_violation_error_message(self) -> dict[str, str]:
            return {"field": "Validation error message"}

- case: get_violation_error_message_invalid
  main: |
    from django.db.models.constraints import BaseConstraint

    class MyConstraint(BaseConstraint):

        def get_violation_error_message(self) -> int:
            return 10
  out: |
    main:5: error: Return type "int" of "get_violation_error_message" incompatible with return type "str | _StrPromise | dict[str, Any]" in supertype "django.db.models.constraints.BaseConstraint"  [override]
